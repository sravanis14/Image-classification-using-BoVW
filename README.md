This notebook is implementation of image classification using Bag of visual Words(BoVW). 

The dataset used in this notebook is Labeled Faces in the Wild (LFW) people dataset (classification) from sklearn.
